package lt.keturka.jonasbank.exceptions;

/**
 * Exception indicates that transfer could not be executed because the debit and credit accounts are the same
 */
public class SameTransferAccountException extends RuntimeException {
}
